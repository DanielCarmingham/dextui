target/
.DS_Store

# Leftover .NET build output from the pre-Rust implementation. The sources are
# gone (see the v0.1.0-dotnet tag); these directories may still exist locally.
bin/
obj/

# Per-machine Claude Code settings
.claude/settings.local.json

# This repo's own dex store. dex resolves it from the cwd, so working on dextui
# from here creates one; it is a local task list, not part of the project.
.dex/
